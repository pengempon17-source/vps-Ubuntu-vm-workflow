name: Ubuntu VPS 6 Hours
on: workflow_dispatch # Menjalankan secara manual

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup tmate session
      uses: mxschmitt/action-tmate@v3
      # Sesi akan tetap berjalan hingga 6 jam atau sampai Anda menghentikannya
      
